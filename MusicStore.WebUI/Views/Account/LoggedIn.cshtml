@*@{
        ViewBag.Title = "LoggedIn";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    @if(Session["UserId"] != null)
    {
        <h4>Hello @Session["UserName"].ToString()</h4>
    }*@

@model MusicStore.Domain.Entities.User

@{
    ViewBag.Title = "SubmitLoginDetails";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 align="center">Welcome</h2>
<p>Thank you for joinin us @Model.UserName</p>
<p>Your details are as follows:</p>
<div>
    <table>
        <tr>
            <td>Your email :</td>
            <td>@Model.Email</td>
        </tr>

    </table>
</div>

