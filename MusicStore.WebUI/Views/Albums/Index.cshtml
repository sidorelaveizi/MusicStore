@*@model IEnumerable<MusicStore.Domain.Entities.Album>*@
@model  IEnumerable<MusicStore.WebUI.Models.AdminViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Albums</h2>
<p>
    @Html.ActionLink("Create New Album", "Create")
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
            <th>
                Title
            </th>
            <th>
                Artist
            </th>
            <th>
                Genre
            </th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.AlbumId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.AlbumId })
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Artist)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Genre)
                </td>
            </tr>
        }
        @*<tr>
            @Html.HiddenFor(model => model.Album.AlbumId)
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = Model.Album.AlbumId }) |
                @Html.ActionLink("Delete", "Delete", new { id = Model.Album.AlbumId })
            </td>
            <td></td>*@

        @*</tr>*@
    </tbody>

</table>
